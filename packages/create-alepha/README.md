# Alepha Create Alepha

Create a new Alepha project with a single command.

## Installation

This package is part of the Alepha framework and can be installed via the all-in-one package:

```bash
npm install alepha
```

