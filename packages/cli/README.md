# @alepha/cache

Alepha Cache is a simple cache abstraction for Alepha.
It provides a simple interface for caching data in memory or using a custom cache provider like Redis.

## Installation

```bash
npm install @alepha/cache
```
