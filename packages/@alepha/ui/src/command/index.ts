/**
 * A command palette, on cmdk.
 *
 * `Command`, `CommandDialog` and their parts. Opt-in: only the shell's spotlight
 * imports it.
 *
 * @module alepha.ui.command
 */

export {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from "./Command.tsx";
