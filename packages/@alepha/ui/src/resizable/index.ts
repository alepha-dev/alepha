/**
 * Resizable panels, on react-resizable-panels.
 *
 * `ResizablePanelGroup`, `ResizablePanel` and `ResizableHandle`. Opt-in:
 * nothing else in `@alepha/ui` imports it.
 *
 * @module alepha.ui.resizable
 */

export {
  ResizableHandle,
  type ResizableHandleProps,
  ResizablePanel,
  type ResizablePanelProps,
  ResizablePanelGroup,
  type ResizablePanelGroupProps,
} from "./Resizable.tsx";
