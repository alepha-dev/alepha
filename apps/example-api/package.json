{
  "private": true,
  "type": "module",
  "name": "example-api",
  "scripts": {
    "dev": "alepha dev",
    "build": "alepha build",
    "start": "node dist",
    "wrangler": "cd dist && wrangler",
    "wrangler:dev": "cd dist && wrangler dev",
    "compile": "cd dist && bun build --compile --minify --bytecode index.js"
  },
  "dependencies": {
    "alepha": "^0.21.1"
  },
  "devDependencies": {
    "vite": "^8.0.13",
    "wrangler": "^4.93.0"
  }
}
