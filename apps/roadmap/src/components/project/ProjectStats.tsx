import { Flex } from "@alepha/react-flex";

const ProjectStats = () => {
	return (
		<Flex fill center>
			<Flex pad2 center bordered rounded>
				TODO: Stats
			</Flex>
		</Flex>
	);
};

export default ProjectStats;
