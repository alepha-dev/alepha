import { Flex } from "@alepha/react-flex";

const ProjectBoard = () => {
	return (
		<Flex fill center>
			<Flex pad2 center bordered rounded>
				Project Board
			</Flex>
		</Flex>
	);
};

export default ProjectBoard;
