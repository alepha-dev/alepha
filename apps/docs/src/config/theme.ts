import type { MantineThemeOverride } from "@mantine/core";

export const theme = {
  mantine: {} as MantineThemeOverride,

  sidebarWidth: 300,
  sidebarBreakpoint: "md",

  headerHeight: { base: 64 },
  footerHeight: 32,
};
