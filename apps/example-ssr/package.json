{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "alepha dev",
    "build": "alepha build --cloudflare",
    "compile": "cd dist && bun build --compile",
    "start": "node dist",
    "wrangler": "cd dist && wrangler",
    "wrangler:dev": "yarn build && cd dist && wrangler dev"
  },
  "dependencies": {
    "alepha": "^0.13.8",
    "react": "^19.2.3"
  },
  "devDependencies": {
    "vite": "^7.3.0",
    "wrangler": "^4.56.0"
  }
}
