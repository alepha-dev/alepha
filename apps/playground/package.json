{
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"start": "node dist/index.mjs",
		"preview": "yarn build && http-server dist/public -p 9854 -b"
	},
	"devDependencies": {
		"http-server": "^14.1.1",
		"rolldown-vite": "^7.0.5"
	}
}
