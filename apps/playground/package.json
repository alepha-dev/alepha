{
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"start": "node dist",
		"build&start": "yarn build && yarn start",
		"preview": "yarn build && vite preview --outDit=dist/public"
	},
	"devDependencies": {
		"@vitejs/plugin-react-oxc": "^0.3.0",
		"rolldown-vite": "^7.0.12"
	}
}
