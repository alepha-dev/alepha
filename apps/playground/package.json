{
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "node --watch src/index.ts",
		"build": "vite build",
		"start": "vite build && node dist",
		"preview": "yarn build && http-server dist/public -p 9854 -b"
	},
	"devDependencies": {
		"http-server": "^14.1.1",
		"rolldown-vite": "^7.0.5"
	}
}
