import { $logger } from "@alepha/logger";

export class Api {
	log = $logger();

	// realm = $realmUsers();
	// google = $authGoogle(this.realm);
	//
	// thinking = $scheduler({
	// 	cron: "*/1 * * * *", // every minute
	// 	handler: async () => {
	// 		this.log.info("Thinking...");
	// 	},
	// });
}
