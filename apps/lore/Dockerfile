# Generated by `alepha image`, from this build's manifest:
#   runtime: node
#   entry: index.node.js
# It is yours now. Edit it freely; `alepha image` reuses it untouched
# and only warns when the facts above stop matching a later build.
FROM node:26-alpine
WORKDIR /app

LABEL "dev.alepha.runtime"="node"
LABEL "org.opencontainers.image.source"="https://github.com/alepha-dev/alepha"
LABEL "org.opencontainers.image.title"="Lore"
LABEL "org.opencontainers.image.description"="Project management and telemetry for Alepha applications."
LABEL "org.opencontainers.image.licenses"="MIT"

COPY --chown=1000:1000 . .

RUN mkdir -p "/data" && chown 1000:1000 "/data"

ENV SERVER_HOST=0.0.0.0
ENV DATA_DIR="/data"
ENV DATABASE_URL="sqlite:///data/lore.db"
ENV APP_SECRET_FILE="/data/.app_secret"
ENV SERVER_PORT="3000"

VOLUME ["/data"]

USER 1000

CMD ["node", "index.node.js"]
