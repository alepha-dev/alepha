{
	"private": true,
	"type": "module",
	"packageManager": "yarn@4.9.1",
	"workspaces": {
		"packages": [
			"apps/*",
			"packages/*"
		]
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@types/node": "^24.0.1",
		"@vitest/coverage-v8": "^3.2.3",
		"@vitest/ui": "^3.2.3",
		"depcheck": "^1.4.7",
		"typescript": "^5.8.3",
		"vitest": "^3.2.3"
	},
	"scripts": {
		"alepha": "node scripts/index.ts",
		"v": "yarn alepha verify",
		"convert": "node scripts/convert.ts",
		"format": "biome format --write",
		"lint": "biome lint --write",
		"check": "tsc --noEmit",
		"test": "vitest run",
		"check-dependencies": "yarn workspaces foreach -Apt --exclude alepha --no-private exec depcheck --ignores=@alepha/testing,shx,vitest,drizzle-kit,swagger-ui-dist --ignore-patterns=dist,assets"
	}
}
