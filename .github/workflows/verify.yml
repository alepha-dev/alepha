name: Verify

permissions:
  contents: read

on:
  # Every branch, because that is how the work is actually done here: an agent
  # works in a worktree on a `claude/**` branch, pushes it, and watches CI.
  # Until 2026-09-09 this was `main` alone, so a branch push ran NOTHING and
  # the only way to prove a branch green was to open a PR - which the working
  # agreement in CLAUDE.md forbids an agent from doing unprompted. A branch
  # could therefore only be proven by merging it.
  #
  # ⚠️ **Nothing here deploys.** This workflow verifies and stops; the deploys
  # live in `deploy-latest.yml`, which triggers on this one succeeding and
  # refuses anything whose head branch is not `main`. That is what makes a
  # branch push safe to run. Before this split the guards were in-file and
  # said `github.event_name != 'pull_request'`, which is TRUE for a push to
  # any branch - on a widened trigger that would have shipped docs, ui and
  # shop to production from every agent worktree.
  push:
    branches:
      - "**"
  # Kept, though same-repo branches no longer need it: a pull_request run is
  # the only way a FORK's code is ever tested, and this repository is public.
  # The cost is that a PR opened by hand from a local branch runs CI twice.
  pull_request:
    branches:
      - main
  workflow_run:
    workflows: ["Release"]
    types:
      - completed

concurrency:
  # Namespaced by event, because `github.ref` for a `workflow_run` is the
  # DEFAULT BRANCH: a Release follow-up used to land in the push-to-main group
  # and cancel that run mid-test, and the push run is the only one that goes on
  # to deploy Lore. Lore silently did not ship, and the evidence was a run
  # marked `cancelled` that reads exactly like "superseded by a newer push".
  # Two Releases in a row still supersede each other: both land in the
  # `workflow_run` group, on the same ref.
  #
  # Per branch, so two agents pushing two branches never cancel each other -
  # only a newer push to the SAME branch does.
  group: verify-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

# ⚠️ **Nothing in this workflow runs a `lore` command any more** - those went
# to `deploy-latest.yml` with the deploy jobs, and this variable is inherited
# by the test jobs alone now.
#
# It stays because removing it is a change to the environment the suite runs
# in, not a tidy-up. `apps/lore`'s push-image spec was written specifically to
# blank it (a spec container merges over `process.env`, so a value set for
# every job reaches the code under test), and that spec is green against this
# being set. Dropping it is its own change, with its own run to prove it.
#
# The paragraphs below are the history of why it is declared once at the top.
#
# It used to come from `lore({ project: "alepha" })` in `alepha.config.ts`,
# which meant it came from whichever directory a step happened to run in.
# `apps/lore`'s config named no project, so its artifact push died on
# `No Lore project named`, `continue-on-error` swallowed it, and the 0.28.0
# release shipped green without Lore's own artifact. A workflow-level default
# is the shape that cannot do that again.
#
# Not a secret: a project slug is in every URL Lore serves. `LORE_API_KEY` is
# the secret, and it stays on the individual steps that need it.
env:
  LORE_PROJECT: alepha

jobs:
  # ──────────────────────────────────────────────────────────────────────
  # The static half of `yarn v`: lint, typecheck and every `check:*`.
  #
  # This job and `test` below are the job that used to be called `verify`,
  # split in two on 2026-09-09. Together they are still exactly `yarn v` minus
  # its build and e2e half, so a red one here is the same red you would have
  # got locally - it just says which half, without waiting for the other. The
  # split is also the shape sharding needs: `test` can fan out across runners
  # without dragging these seven cheap steps through every one of them.
  #
  # Measured on run 34291618784, where this was all one job: these steps are
  # 117s of it and `yarn alepha test` is 870s.
  #
  # ⚠️ The service containers are kept, and kept UNPROVEN. Nothing in these
  # steps obviously wants postgres, but `check:migrations` boots each app's
  # server entry through `loadAlephaFromServerEntryFile`, and whether that
  # connects has never been tested: this half has always run with the
  # containers already up. Dropping them is worth ~16s and is a deliberate
  # experiment, not a tidy-up to smuggle into a split.
  # ──────────────────────────────────────────────────────────────────────
  checks:
    runs-on: ubuntu-latest
    # A hang detector, not a budget. Public-repo minutes are free and the
    # GitHub cap is 6 hours, so a generous number costs nothing while a tight
    # one costs a blocked deploy: a job killed on the timeout reports as the
    # failure the deploys gate on.
    timeout-minutes: 30
    services:
      redis:
        image: redis
        ports:
          - 16379:6379

      postgres:
        image: postgres
        ports:
          - 15432:5432
        env:
          POSTGRES_DB: postgres
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres

      s3mock:
        image: adobe/s3mock
        ports:
          - 19090:9090

    steps:
      - name: Git Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup
        uses: ./.github/actions/setup

      - name: Clean
        run: yarn clean

      - name: Prepare
        run: yarn copy

      - name: Lint Project
        run: yarn lint

      - name: TypeScript Check
        run: yarn typecheck

      - name: Dependencies Check
        run: yarn check:deps

      - name: i18n Catalog Check
        run: yarn check:i18n

      - name: DB Migrations Check
        run: yarn check:migrations

      - name: Conventions Check
        run: yarn check:conventions

      - name: Docs Check
        run: yarn check:docs

  # ──────────────────────────────────────────────────────────────────────
  # The unit and integration suite - `yarn v`'s longest single step, and the
  # other half of the old `verify` job.
  #
  # Sharded six ways. `alepha test` appends `VITEST_ARGS` last on the vitest
  # command line, so this needs no code change on the CLI side - the flag
  # reaches vitest as `vitest run --shard=i/6`.
  #
  # ⚠️ **Six is the ceiling, and it is not a round number - it is where this
  # job stops being the critical path.** Measured on run #1691, which ran
  # this at four: the shards came in at 5.7 / 6.5 / 6.2 / 6.2min against
  # 20.3min unsharded, which splits into ~89s of fixed setup per shard and
  # ~1192s of shardable work. So six lands at ~4.8min and eight at ~4.0min -
  # and `e2e-apps` sits at 6.2min, unsharded, with a floor it cannot leave
  # while every e2e job rebuilds the world. Past six the extra shards finish
  # early and wait.
  #
  # Raising it again is therefore the wrong lever twice over: it buys no wall
  # clock, and it costs 89s of setup per added shard plus one more draw from
  # a flake distribution this repository knows is environmental. The lever
  # that IS left is the 149s `yarn build` in the three e2e jobs, none of
  # which needs every workspace built.
  #
  # ⚠️ Vitest shards by sha1 of the spec's path, sorted, sliced into equal
  # COUNTS - not by duration, and not per project. With 868 spec files that
  # balances well (the four shards above spread 0.8min); it would not for a
  # handful of files, and vitest refuses outright when the shard count
  # exceeds the file count.
  #
  # Coverage is NOT measured here. It lives in `coverage.yml`, nightly, and
  # that file carries the reasoning: Lore keeps one quality row per branch per
  # day, so eighteen of the ~19 pushes a day were overwritten before anyone
  # read them, and the instrumentation was costing ~2min on the critical path
  # to the deploys to produce them. Read `coverage.yml` before moving it back.
  # ──────────────────────────────────────────────────────────────────────
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      # A failing shard must not cancel its siblings. The point of running the
      # suite is to learn everything that is broken, and `fail-fast: true`
      # (the default) turns four shards into "the first one to fail", which is
      # strictly less than the single job this replaced told you.
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6]
    services:
      redis:
        image: redis
        ports:
          - 16379:6379

      postgres:
        image: postgres
        ports:
          - 15432:5432
        env:
          POSTGRES_DB: postgres
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres

      s3mock:
        image: adobe/s3mock
        ports:
          - 19090:9090

    steps:
      - name: Git Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup
        uses: ./.github/actions/setup

      - name: Clean
        run: yarn clean

      - name: Prepare
        run: yarn copy

      # No `--coverage`. That flag is what `coverage.yml` runs nightly, and it
      # carries five of the nine numbers Lore stores, not four: it also turns
      # on the JSON test reporter. Nothing in this job reads either report.
      - name: Test Project
        run: yarn alepha test
        env:
          VITEST_ARGS: --shard=${{ matrix.shard }}/6

      # Here rather than in `checks`, because two of the six bun specs are
      # `BunPostgresProvider` and `BunRedisProvider`: they want the service
      # containers above. It costs 3s, so it rides with the suite it belongs
      # to rather than buying `checks` a reason to keep the containers.
      #
      # On one shard only: `bun test` takes no `--shard`, so without this the
      # same six specs would run four times and a failure would report four
      # times over.
      - name: Test Bun Providers
        if: matrix.shard == 1
        run: yarn test:bun

  # ──────────────────────────────────────────────────────────────────────
  # Build + E2E, as three jobs. The long pole of the workflow, and the half
  # of `yarn v` the two jobs above skip.
  #
  # One job until 2026-09-09, where `yarn e2e` ran every app's suite through
  # `foreach -p` and then `yarn e2e-cli` after it. Measured on run
  # 34291618784: 653s for the suites, of which **lore alone was 10.8 minutes**
  # (171 tests, 2 workers) and every other app finished inside 2.1 minutes.
  # One suite was the whole wall clock, so one suite gets its own job - and
  # with it, the handle sharding needs (`playwright test --shard=i/N`, which
  # `apps/lore` is already safe for: `fullyParallel` with one Lore instance
  # per worker and no shared `webServer`).
  #
  # All three pay `yarn build` (149s) rather than sharing one through an
  # artifact, and that is arithmetic, not laziness: a serialised build job
  # costs its own ~82s of setup before anything can start, which is more than
  # the download saves. Three builds in parallel are cheaper than one in front.
  #
  # The suites are bounded on the STEP, not just the job. Playwright's
  # `globalTimeout` covers the tests but not teardown, and teardown is where
  # this hangs: a failing run once printed its results and then sat five
  # minutes holding orphaned wrangler and browser processes until the job cap
  # killed everything, which reads as "CI is slow" rather than "two tests
  # failed".
  #
  # No service containers on any of them, deliberately. Only `vitest.config.ts`
  # points at those, and that is `test` above: every app these jobs build is
  # SQLite or nothing (shop injects `:memory:`, lore and docs inject no
  # `DATABASE_URL` at all, and `alepha/orm` picks SQLite unless the URL starts
  # with `d1:`). Nothing here references REDIS_URL or S3_ENDPOINT, and
  # `node dist` does not read `.env`, so the containers were unreachable
  # rather than merely unused.
  # ──────────────────────────────────────────────────────────────────────
  e2e-apps:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Git Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup
        uses: ./.github/actions/setup

      - name: Clean
        run: yarn clean

      - name: Prepare
        run: yarn copy

      - name: Build Project
        run: yarn build

      - name: Setup Playwright
        uses: ./.github/actions/playwright

      # `yarn e2e` minus lore: docs, ui, examples/shop and examples/ssr.
      #
      # ⚠️ **`--exclude alepha-monorepo` is load-bearing and is NOT tidy-up.**
      # `-A` includes the repository root, the root workspace has an `e2e`
      # script of its own, and that script is `foreach -Apt run e2e` with no
      # exclusions - so dropping this flag makes the step run every suite a
      # second time, lore included, both copies at once. That is not a
      # theoretical risk: it is what the first version of this job did.
      # Two concurrent `docs` runs then raced for the same derived port and
      # the step died on `EADDRINUSE` after 11 minutes, which reads as a
      # flaky port allocator rather than as a recursive foreach.
      #
      # Yarn's own recursion guard does not cover this. It is keyed on the
      # workspace AND the script name, so it saves `yarn e2e` (root `e2e`
      # calling root `e2e`) and saves nothing here, where the caller is a
      # shell rather than the script being called. A root `e2e:apps` script
      # was tried first for exactly that reason and recursed the same way.
      #
      # `--exclude lore` matches the ident, so it drops `apps/lore` and
      # leaves `packages/@alepha/lore` (which has no `e2e` script) alone.
      - name: Run E2E Tests
        timeout-minutes: 20
        run: yarn workspaces foreach -Apt --exclude lore --exclude alepha-monorepo run e2e

  e2e-lore:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      # Same reasoning as `test` above: a red shard must not hide its
      # siblings' results.
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6]
    steps:
      - name: Git Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup
        uses: ./.github/actions/setup

      - name: Clean
        run: yarn clean

      - name: Prepare
        run: yarn copy

      - name: Build Project
        run: yarn build

      - name: Setup Playwright
        uses: ./.github/actions/playwright

      # Sharded six ways, which `apps/lore` is already safe for: it is
      # `fullyParallel` with one Lore instance per worker and no shared
      # `webServer`, so a shard shares nothing with its siblings and they are
      # on separate runners anyway.
      #
      # ⚠️ This job is floor-bound, not work-bound, so it scales far worse
      # than `test` does and the ceiling argument there applies harder here.
      # At four (run #1691) the shards were 7.4 / 5.7 / 6.1 / 7.2min against
      # 14.8min unsharded: only 2x, because ~218s of each shard is checkout,
      # setup, `yarn copy`, `yarn build` and the browser install before one
      # test runs. Six takes it to ~6.3min and eight would take it to ~5.5,
      # which is under `e2e-apps` and therefore worth nothing.
      #
      # ⚠️ Playwright shards by test COUNT, and lore's tests vary a lot in
      # duration - hence the 5.7 to 7.4 spread above from an even split of
      # 174 tests. Smaller shards make that imbalance relatively worse, so
      # the projection above is the optimistic end.
      #
      # ⚠️ `apps/lore/playwright.config.ts` sets `globalTimeout: 900_000`, and
      # that number was sized against this suite running alongside four others
      # on one runner. It now bounds a QUARTER of the suite on a runner of its
      # own, so it is generous rather than wrong - the safe direction, but no
      # longer the measurement its comment describes. Re-read that comment
      # before tightening it.
      - name: Run E2E Tests
        timeout-minutes: 20
        run: yarn w lore e2e --shard=${{ matrix.shard }}/6

  # Its own job for the reason it was already its own step: `e2e-cli` packs
  # the workspace with `yarn pack` and installs the tarball into a throwaway
  # project, so a failure here means a packaging bug rather than a UI one.
  # No Playwright - it drives the CLI, not a browser.
  e2e-cli:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Git Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup
        uses: ./.github/actions/setup

      - name: Clean
        run: yarn clean

      - name: Prepare
        run: yarn copy

      # The tarball carries `dist/`, so the build has to have happened: see
      # the header of `apps/e2e-cli/src/lore.e2e.spec.ts`, which fails with a
      # named error rather than a mysterious one when it has not.
      - name: Build Project
        run: yarn build

      # The Bay end-to-end in e2e-cli builds the real bay binary natively
      # (apps/e2e-cli/src/bay.e2e.spec.ts) and fails loudly without go, on
      # purpose: a skipped Go test is how a green run lies. The bay job has Go
      # and no Node, this one has Node and no Go, so Go is added here.
      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: apps/bay/go.mod
          cache-dependency-path: apps/bay/go.sum

      - name: Run CLI E2E Tests
        timeout-minutes: 10
        run: yarn e2e-cli

  # ──────────────────────────────────────────────────────────────────────
  # The self-hosted Lore image. `docker run -p 3000:3000 -v lore:/data <image>`
  # is the whole promise of that image, and nothing in `yarn v` exercises it:
  # a build producing an image that cannot boot is exactly as green as one
  # that works.
  #
  # Here rather than in the Release workflow, because the release only builds
  # the image when someone dispatches it and the push step sits AFTER
  # `npm publish` - so a broken Dockerfile would be found on release day, past
  # the irreversible half of the pipeline. Here rather than in `e2e-cli`,
  # because that runs inside `yarn v` and a docker build does not fit the
  # 10-minute budget. The runners already have a docker daemon (the `checks`
  # and `test` jobs' `services:` need one) and public-repo minutes are free.
  #
  # ⚠️ **`deploy-lore-production` used to deliberately NOT gate on this job,
  # and as of the 2026-09-09 workflow split it does.** That job ships a
  # Cloudflare Worker and touches nothing this one builds, so keeping it out of
  # `needs` meant a docker flake or a slow image build could never block a
  # production deploy with no relationship to the image.
  #
  # `deploy-latest.yml` gates on this WORKFLOW's conclusion, which is atomic:
  # Verify passed or it did not. There is no way to say "all of it except
  # docker" across a workflow boundary. So the exception is gone, and a docker
  # flake now costs a deploy that the next push recovers. If that ever bites,
  # the fix is to move this job and `bay` into a workflow of their own - not to
  # weaken the condition in `deploy-latest.yml`.
  # ──────────────────────────────────────────────────────────────────────
  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Git Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup
        uses: ./.github/actions/setup

      - name: Prepare
        run: yarn copy

      # `alepha build --target=docker --image`: the bundle, the generated
      # Dockerfile and `docker build` in one step, with the tag and the OCI
      # labels coming from `apps/lore/alepha.config.ts`.
      - name: Build the image
        run: yarn w lore build:docker

      - name: Smoke test the image
        run: bash scripts/docker-smoke.sh

      # ⚠️ Both are needed by the size check below, and by nothing else here.
      # The generated Dockerfile is not RUN-free: `mkdir -p /data && chown`
      # prepares the volume, so building for the other architecture executes
      # a shell command under emulation and needs the binfmt handlers.
      # `setup-buildx` is separately required because the default `docker`
      # driver refuses a multi-platform build outright.
      - name: Set up QEMU
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v4.3.0

      - name: Set up Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      # Builds BOTH architectures, which is the only place outside a
      # dispatched Release where the multi-arch build the release performs is
      # exercised at all. The size gate rides along on the result.
      - name: Check the image size budget
        run: node scripts/docker-size.ts

  # ──────────────────────────────────────────────────────────────────────
  # Bay - the Go control plane. Nothing in the yarn pipeline compiles or tests
  # it, which is how a `.gitignore` pattern that swallowed the whole
  # `cmd/bay/` directory reached a commit unnoticed.
  #
  # Cross-compiles as well as builds: Bay is developed on macOS and only ever
  # runs on Linux, so a Linux-only compile error would otherwise surface on
  # the VPS rather than here.
  # ──────────────────────────────────────────────────────────────────────
  bay:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: apps/bay

    steps:
      - name: Git Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          # Read from go.mod so the version lives in exactly one place.
          go-version-file: apps/bay/go.mod
          cache-dependency-path: apps/bay/go.sum

      - name: Verify Formatting
        # `gofmt -l` lists misformatted files and exits 0 either way, so the
        # test has to be on its output.
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "Not gofmt'd:"; echo "$unformatted"; exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Test
        run: go test ./...

      - name: Cross-compile for the targets Bay actually runs on
        run: |
          for target in amd64 arm64; do
            echo "→ linux/$target"
            GOOS=linux GOARCH=$target CGO_ENABLED=0 go build -o /dev/null ./cmd/bay
          done
